<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.okta.android.samples.custom_sign_in">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PasswordRecoverActivity"
            android:label="@string/password_recover"/>
        <activity android:name=".DeepLinksHandlerActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="${hostName}" android:scheme="https" android:pathPrefix="/" />
            </intent-filter>
        </activity>
        <activity android:name=".RecoveryQuestionActivity"
            android:label="@string/security_question"/>
        <activity android:name=".PasswordResetActivity"
            android:label="@string/password_reset"/>
    </application>

</manifest>